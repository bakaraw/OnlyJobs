import 'package:flutter/material.dart';

class ClientOrEmployee extends StatelessWidget {
  ClientOrEmployee({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
